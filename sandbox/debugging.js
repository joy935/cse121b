const PI = 3.14;
let radius = 3;
let area = 0;

function circleArea(radius) {
    radius * radius * PI
}
area = radius * radius * PI;
console.log("area1:", area)
radius = 4;
area = radius * radius * PI;
console.log("area2:", area)
